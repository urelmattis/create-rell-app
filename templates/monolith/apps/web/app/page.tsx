// @ts-nocheck -- template-only; the scaffold engine strips this line before writing to user projects so scaffolded output has normal TypeScript checking.
export default function HomePage() {
  return (
    <main>
      <h1>{{projectName}}</h1>
      <p>
        Welcome to your scaffolded monolith. Sign-in and billing pages will appear after Story 2.3
        and Epic 3 add them. Edit <code>web/app/page.tsx</code> to customize this landing page.
      </p>
    </main>
  );
}
