# Architecture decision records

One file per decision, numbered `NNNN-<slug>.md`. `/domain-modeling` (via `/grill-with-docs`) writes them when a decision is actually made; nothing here is written speculatively.
