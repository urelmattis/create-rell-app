# Claude Code OAuth token: run `claude setup-token` on this machine and paste
# the printed token here. The sandbox uses your Claude subscription with it.
CLAUDE_CODE_OAUTH_TOKEN=
# No GitHub token is needed: the host fetches the issue with your gh login,
# and pushes the branch and opens the pull request afterwards.
